# aws-xray-recorder-sdk-core

## How to build
Just run build.sh.
```
./build.sh
```
Then, new jars are created in target dir.

## How to publish new jar
`lein deploy` would work for you.
```
lein deploy clojars
```

Some configurations might be needed.
See leiningen's page for the details.
